/*
 * Webinar sign-ups shown on the Resources page under the guides. This is
 * the only file you edit to add one. The block does not render while this
 * list is empty, so nothing shows until there is something to show.
 *
 * Each entry takes:
 *   title      what the session is called
 *   summary    one or two sentences on what it covers
 *   when       'Every Thursday, 12pm UK' or a date. Shown as a small label.
 *   signupUrl  the registration link. Opens in a new tab.
 *
 * Example:
 *   {
 *     title: 'Ask Damon Anything',
 *     summary: 'Live questions on AI in practice, every week.',
 *     when: 'Weekly',
 *     signupUrl: 'https://...',
 *   },
 */
export const webinars = [];
